//Global

// LEAKED GLOBALS occur when you a variable is uses before being defined with a (var) or (let) datatype. The system implicitly creates a global variable to handle the lack of explicit declaration.

//Local
    //Local
//Local

//let name = 'Bill'
if (true) {
   
   // let name = 'Mike'
    
    if(true){
       let name = ' Jen';
        console.log (name);
    }
}

if (true){
    console.log(name);
}